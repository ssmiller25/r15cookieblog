image:
  file: .gitpod.Dockerfile

# List the ports to expose. Learn more https://www.gitpod.io/docs/config-ports/
ports:
  # Direct Hugo
  - port: 1313
    onOpen: open-preview
  # Docker container
  - port: 8080
    onOpen: open-preview
